'use client';import Link from 'next/link';import {useState} from 'react';import {useRouter} from 'next/navigation';import {api} from '@/lib/api';export default function Login(){const r=useRouter();const [e,setE]=useState('student@example.com');const [p,setP]=useState('Password123!');const [err,setErr]=useState('');async function submit(x:any){x.preventDefault();try{const d=await api('/auth/login',{method:'POST',body:JSON.stringify({email:e,password:p})});localStorage.setItem('token',d.access_token);localStorage.setItem('user',JSON.stringify(d.user));r.push('/dashboard')}catch(ex:any){setErr(ex.message)}}return <div className="flex min-h-screen"><div className="flex flex-1 items-center justify-center p-8"><div className="w-full max-w-md"><div className="text-3xl font-bold">DriveBook</div><h1 className="mt-10 text-2xl font-bold">Sign in to your account</h1><p className="mt-2 text-sm text-gray-500">Book lessons, track progress and receive instructor feedback.</p>{err?<div className="mt-5 rounded-lg bg-red-50 p-3 text-sm text-red-700">{err}</div>:null}<form onSubmit={submit} className="mt-8 space-y-5"><label className="block text-sm font-medium">Email<input className="input mt-2" value={e} onChange={x=>setE(x.target.value)} type="email"/></label><label className="block text-sm font-medium">Password<input className="input mt-2" value={p} onChange={x=>setP(x.target.value)} type="password"/></label><button className="btn btn-primary w-full">Sign in</button></form><p className="mt-6 text-sm text-gray-600">New learner? <Link className="font-semibold text-blue-600" href="/register">Create an account</Link></p></div></div><div className="hidden flex-1 bg-gradient-to-br from-blue-50 via-white to-amber-50 lg:flex lg:items-center lg:justify-center"><div className="max-w-lg p-12"><div className="rounded-3xl border bg-white p-8 shadow-sm"><p className="text-sm font-semibold text-blue-600">LEARN • PRACTISE • PROGRESS</p><h2 className="mt-3 text-4xl font-bold tracking-tight">A clear path from your first lesson to test day.</h2><p className="mt-4 text-gray-600">See instructor availability, book securely and keep every lesson’s feedback in one place.</p></div></div></div></div>}