'use client';import {useEffect,useState} from 'react';import Shell from '@/components/Shell';import {api} from '@/lib/api';export default function Bookings(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api('/bookings').then(setRows)},[]);return <Shell><h1 className="text-3xl font-bold">Bookings</h1><p className="mt-2 text-gray-600">Upcoming and completed driving lessons.</p><div className="mt-8 card overflow-hidden"><div className="divide-y">{rows.map(b=><div key={b.id} className="grid gap-3 p-5 md:grid-cols-[1.4fr_1fr_1fr_auto]"><div><div className="font-semibold">{new Date(b.starts_at).toLocaleString()}</div><div className="text-sm text-gray-500">{b.instructor_name}</div></div><div><div className="text-xs text-gray-500">Booking</div><span className="badge bg-blue-50 text-blue-700">{b.status}</span></div><div><div className="text-xs text-gray-500">Payment</div><span className="badge bg-green-50 text-green-700">{b.payment_status}</span></div><a href={`/feedback?booking=${b.id}`} className="btn btn-secondary">Feedback</a></div>)}{rows.length===0?<div className="p-6 text-gray-500">No bookings yet.</div>:null}</div></div></Shell>}