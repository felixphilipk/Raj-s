'use client';import Link from 'next/link';import {useState} from 'react';import {useRouter} from 'next/navigation';import {api} from '@/lib/api';export default function Register(){const r=useRouter();const [f,setF]=useState({first_name:'',last_name:'',email:'',phone:'',password:''});const [err,setErr]=useState('');async function submit(e:any){e.preventDefault();try{const d=await api('/auth/register',{method:'POST',body:JSON.stringify(f)});localStorage.setItem('token',d.access_token);localStorage.setItem('user',JSON.stringify(d.user));r.push('/dashboard')}catch(x:any){setErr(x.message)}}return <div className="min-h-screen bg-stone-50 p-6"><div className="mx-auto mt-10 max-w-xl card p-8"><Link href="/" className="text-xl font-black">RAJ<span className="text-teal-700">.</span></Link><div className="mt-8 text-2xl font-bold">Create your learner account</div><p className="mt-2 text-sm text-gray-500">Use your details to book and manage lessons.</p>{err?<div role="alert" className="mt-5 rounded-lg bg-red-50 p-3 text-sm text-red-700">{err}</div>:null}<form onSubmit={submit} className="mt-8 grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium">First name<input required className="input mt-2" onChange={e=>setF({...f,first_name:e.target.value})}/></label><label className="text-sm font-medium">Last name<input required className="input mt-2" onChange={e=>setF({...f,last_name:e.target.value})}/></label><label className="text-sm font-medium sm:col-span-2">Email<input required type="email" className="input mt-2" onChange={e=>setF({...f,email:e.target.value})}/></label><label className="text-sm font-medium sm:col-span-2">Phone<input className="input mt-2" onChange={e=>setF({...f,phone:e.target.value})}/></label><label className="text-sm font-medium sm:col-span-2">Password<input required type="password" minLength={8} className="input mt-2" onChange={e=>setF({...f,password:e.target.value})}/></label><button className="btn btn-primary sm:col-span-2">Create learner account</button></form><p className="mt-6 text-sm">Already registered? <Link href="/login" className="font-semibold text-teal-700">Sign in</Link></p><p className="mt-3 text-sm text-gray-600">Are you an instructor? <Link href="/register/instructor" className="font-semibold text-teal-700">Create an instructor account</Link></p></div></div>}
